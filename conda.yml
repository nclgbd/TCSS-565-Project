name: tcss565
channels:
  - defaults
  - conda-forge
dependencies:
  - pip
  - pip:
      - folium
      - geopandas
      - geopy
      - hydra-core
      - mapclassify
      - matplotlib
      - rich
      - tqdm
